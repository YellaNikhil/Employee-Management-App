import React from 'react';
import FormComponent from './FormComponent';

const UpdateEmployeeComponent = (props) => {
    return (
        <FormComponent heading="Update Employee" buttonName="Update"/>
    );
}

export default UpdateEmployeeComponent;